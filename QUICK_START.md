# Быстрый запуск проекта

## ❌ Текущий статус

Проверка показала, что на вашем компьютере:
- ✅ Node.js установлен (v22.17.1)
- ❌ Docker не установлен или не в PATH
- ❌ Python не установлен или не в PATH

## 📦 Что нужно установить

### 1. Docker Desktop (для базы данных)
1. Скачайте: https://www.docker.com/products/docker-desktop/
2. Установите и запустите Docker Desktop
3. Перезапустите терминал после установки

### 2. Python 3.14+ (для бэкенда)
1. Скачайте: https://www.python.org/downloads/
2. При установке отметьте "Add Python to PATH"
3. Или используйте `uv` (рекомендуется): https://github.com/astral-sh/uv

## 🚀 После установки - команды для запуска

### Терминал 1: База данных
```powershell
docker compose up -d impulse_db
```

### Терминал 2: Бэкенд
```powershell
cd back
uv sync
uv run uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Или с pip:
```powershell
cd back
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### Терминал 3: Фронтенд
```powershell
cd web
npm install
npm start
```

## ✅ Проверка работы

- База данных: `docker compose ps` (должен показать running)
- Бэкенд: http://localhost:8000/docs (должна открыться документация API)
- Фронтенд: http://localhost:3000 (должно открыться приложение)

## 🔧 Альтернатива: запуск без Docker

Если не хотите устанавливать Docker, можно использовать локальную PostgreSQL:
1. Установите PostgreSQL: https://www.postgresql.org/download/windows/
2. Создайте базу данных `postgres`
3. Обновите `back/database/db.py` - измените порт с 5434 на 5432





